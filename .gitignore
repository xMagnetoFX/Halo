# deps
node_modules/

# builds
dist/
build/
.expo/
*.tsbuildinfo

# packaged release artifacts (APK, Windows build tree); rebuild, don't commit
/Release/

# native (generated by expo prebuild; regenerate with `pnpm --filter @halo/mobile exec expo prebuild`)
apps/mobile/ios/
apps/mobile/android/

# design exploration artifacts (HTML mockups; implementation is the source of truth)
apps/mobile/design/

# env & data
.env
.env.*
!.env.example
*.sqlite
*.sqlite-journal
data/

# os / editor
.DS_Store
.idea/
.vscode/*
!.vscode/settings.json
